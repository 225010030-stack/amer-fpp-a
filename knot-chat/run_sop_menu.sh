#!/usr/bin/env bash
# 打印 SOP 主菜单：同事发「主菜单」时在 chat 中展示
set -euo pipefail
# shellcheck disable=SC1091
source "$(dirname "$0")/_common.sh"

cat <<'EOF'
========================================
  AMER FPP 账单助手 · 主菜单（未来账单 SOP）
========================================

请回复数字或关键词，我将告诉你需要什么文件并执行对应步骤。
所有结果以脚本 DONE: 路径为准，不会编造。

【0】US演示 / 四痛点演示
    → 用内置样例跑通全流程（新同事首选，无需准备文件）

【环节1 Source · 材料就绪】
【1】目录预检
    需要：USA/CAN 下当月目录结构（或指定月份 202606）
    输出：目录预检输出/预检报告_*.md
    指令：目录预检

【环节2 Process · 分摊计算】
【2】自动分摊（痛点1）
    需要：成本中心人数输入.csv
    参数：期间(202606)、国家(US/CAN)、账单总额
    输出：上传输出/HC_Analysis_US_*.csv
    指令：自动分摊
    ★ 自有文件：先上传到 上传输入/ 再告诉我路径

【3】HC校验
    需要：HC_Analysis_*.csv（上一步产出或你上传的）
    输出：HC校验输出/HC校验报告_*.md
    指令：HC校验

【环节3 Submit · 提单产出】
【4】生成提单文本块（痛点2）
    需要：提单预填表.csv
    输出：上传输出/FPP提单文本块_*.md
    指令：生成提单文本块

【环节4 Exception · 提交前阻断】
【5】检查成本中心（痛点3）
    需要：Source(HC Analysis) + Status(成本中心状态表)
    输出：上传输出/成本中心有效性检查报告_*.md
    指令：检查成本中心

【环节5 Output · 台账归档】
【6】台账更新（痛点4）
    需要：Prefill(提单预填表) + Ledger(台账模板或实际台账)
    输出：上传输出/台账_自动更新_*.csv
    指令：台账更新

【环节6 · 一键检查】
【7】总检查 = 目录预检 + HC校验

----------------------------------------
推荐月度闭环顺序：4 → 2 → 3 → 5 → 6 → 4
  (提单文本 → 分摊 → HC校验 → 成本中心 → 台账 → 再确认提单文本)

需要上传多个 CSV？回复「网页入口」获取 upload-docs 图形界面说明。
回复「帮助」看全部指令。
========================================
EOF
